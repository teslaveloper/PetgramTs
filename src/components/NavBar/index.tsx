import React from 'react';
import { Nav } from './styles';
export const NavBar = () => {
  return (
      <Nav>
        <button>home</button>
        <button>favs</button>
        <button>user</button>
      </Nav>
  )
}
